= form_with(model: train, local: true) do |form|
  = render 'layouts/error', object: train
  .field
    = form.label :number
    = form.text_field :number, id: :train_number
    = form.select :route_id, options_from_collection_for_select(@routes, :id, :name)

  .actions
    = form.submit
