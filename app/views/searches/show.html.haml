%h1 Search result:
-if @result.nil? || @result.count == 0
  = image_tag('no_result', size: "100x100")
  %p No result
-else
  .col-lg-12
    %table
      %tr
        %th Train number:
        %th Arrival time:
        %th Departure time:
        %th Route:
        - @result.each do |train|
          %tr
            .col-lg-6
              %td= link_to train['number'], train_path(train['id'])
              %td= train['arrival_time']
              %td= train['departure_time']
              %td= train['name']

=link_to 'New search', new_search_path
